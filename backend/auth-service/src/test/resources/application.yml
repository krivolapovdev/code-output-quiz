spring:
  profiles:
    active: test
  cloud:
    config:
      enabled: false
      fail-fast: false
  flyway:
    enabled: true
management:
  tracing:
    enabled: false
logging:
  config: classpath:logback-test.xml
