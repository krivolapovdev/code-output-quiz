spring:
  cloud:
    config:
      override-system-properties: false
management:
  endpoints:
    web:
      exposure:
        include: refresh, health, info, prometheus
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
